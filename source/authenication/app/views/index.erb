<div class="container">
  <% if @logged_in %>
    <% @users.each do |user| %>
      <div class="user-display">
        <p><%= user.name %></p>
        <p><%= user.email %></p>
        <p><%= user.access_level %></p>
      </div>
    <% end %>
  <% else %>
    <h2>Welcome!</h2>
  <% end %>
</div>
